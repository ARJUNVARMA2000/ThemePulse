[build]
# Railway monorepo — each service specifies its own root directory

[[services]]
name = "backend"
root = "backend"

[services.build]
dockerfilePath = "Dockerfile"

[services.deploy]
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[[services]]
name = "frontend"
root = "frontend"

[services.build]
dockerfilePath = "Dockerfile"

[services.deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
